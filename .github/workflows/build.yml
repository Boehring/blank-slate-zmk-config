on:
  push:
    branches:
      - main
      - default
  pull_request:
    branches:
      - main
      - default
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
